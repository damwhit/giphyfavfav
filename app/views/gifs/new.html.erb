<%= form_for(@category) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <!-- type in category name -->
  <!-- send api call with category name -->
  <!-- create gif by entering url from api into image_path -->
  <%= f.submit "Create Gif" %>
<% end %>
